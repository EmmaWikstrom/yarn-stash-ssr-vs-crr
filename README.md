# yarn-stash-ssr-vs-csr
A small project exploring the difference between server-side rendering and client-side rendering using HTML, CSS and JavaScript. The same Yarn Stash page is built twice, once with static HTML and once rendered dynamically in the browser, to compare performance, SEO and user experience.
